before:
  hooks:
    - go generate ./...
builds:
  - main: .
    binary: quickrank
    ldflags:
      - -s
      - -w
      - -extldflags
      - -static
    env:
      - CGO_ENABLED=0
    goarch:
      - amd64
      - arm64
      - riscv64
    goos:
      - linux
      - windows
      - darwin
    ignore:
      - goos: windows
        goarch: arm64
    mod_timestamp: "{{ .CommitTimestamp }}"
checksum:
  name_template: '{{ .ProjectName }}_{{ .Version }}_SHA256SUMS'
  algorithm: sha256
gitea_urls:
  api: https://tyr.codes/api/v1
  download: https://tyr.codes
  # set to true if you use a self-signed certificate
  skip_tls_verify: false